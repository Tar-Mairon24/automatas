program prueba?;
variables
string cadena#,

 var%,v_% : real;
 numero&, nu2& : int ;
logico$,  log_1_$ : bool ;
begin
x% = ( numero&/1)* 367;
//esto es un comentario //

If (log_1_$ <= numero&& ) then
begin
v_% := 10.34;
( log_1_& && logico&)
(var% || !nu2&);
end
else
begin
read( n2&);
end
repeat until
repetir
write (logico$) ;
while  ( ) do
while  ( ) do

end.