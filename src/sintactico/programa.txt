program prog?;
var
int : uno&, dos&, tres&;
real : tres%, cuatro% ;
string : cuatro#;
bool : cinco$;

begin

uno& := 10 + ( 39 * dos& ) ;
cinco$ := ( uno& <= 10 ) && (false == cinco$);
cinco$ := (10  * (89 - 100) ) >= ( dos& ) ;
dos& := 1;
dos& := dos& * (19-87.52)/ 52;
write (dos&); cinco$ := false;
cinco& == true;
read ( tres% );
write(dos&);

end
